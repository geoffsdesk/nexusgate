apiVersion: v1
kind: Service
metadata:
  name: contract-engine
  namespace: nexusgate
  labels:
    app.kubernetes.io/name: contract-engine
    app.kubernetes.io/component: contracts
    app.kubernetes.io/part-of: nexusgate
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: contract-engine
  ports:
    - name: http
      port: 8082
      targetPort: http
      protocol: TCP
